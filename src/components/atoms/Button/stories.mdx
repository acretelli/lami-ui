import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs'
import { withDesign } from 'storybook-addon-designs'
import { Button, Props } from './'

<Meta title="MDX/Button" component={Button} />

export const Template = (args) => <Button {...args} />

# Button

Let's define a story for our `Button` component:

<Canvas>
  <Story
    name="My Button"
    decorators={[withDesign]}
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/GKS28OjXFb4FBfT4Imdsdm/Lami?node-id=80%3A2'
      }
    }}
    args={{ children: 'I am in MDX' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="My Button" />

I want extra text description here!!!

<Canvas>
  <Story name="Secondary" args={{ children: 'I am in MDX', variant: 'secondary' }}>
    {Template.bind({})}
  </Story>
</Canvas>